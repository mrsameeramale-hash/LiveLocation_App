
name: location
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI & Animations
  shimmer: ^3.0.0
  lottie: ^3.1.0
  tutorial_coach_mark: ^1.2.0
  overlay_tooltip: ^0.2.0

  # Device & Location
  geolocator: ^11.0.0
  permission_handler: ^11.3.0
  device_info_plus: ^10.1.0

  # Storage
  shared_preferences: ^2.2.2

  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
